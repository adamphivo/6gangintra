{% block postStat %}
<style>
    .statsContainer {
        display: flex;
        flex-direction: column;
        flex-wrap: none;
        justify-content: space-evenly;
        text-align: center;
    }

    .leaderBoard {
        font-family: var(--logo-font);
        margin-top: 1em;
    }

    .leaderBoard ol {
        list-style-type: disc;
        padding: 0;
        margin-top: 1em;
        margin-bottom: 1em;
    }

    .latestPosts {
        font-family: var(--logo-font);
    }

    .latestPosts ol {
        width: 60%;
        margin-left: auto;
        margin-right: auto;
        background-color: #182533;
        box-shadow: 1px 1px 8px black;
        border-radius: 5px;
        font-family: monospace;
        padding-top: 1em;
        padding-bottom: 2em;

    }

    .postTitle {
        text-align: left;
    }

    .date {
        text-align: left;
        margin: 0;
        padding: 0;
        font-size: 0.6em;
    }

    .lPuserName {
        color: rgb(66, 108, 245);
    }

    .lPtitle {
        background: whitesmoke;
        color: black;
        padding: 0.4em;
    }

</style>

<div class="statsContainer">
    <div class="latestPosts">
        <h2>Latest Posts</h2>
            <ol>
                {% for post in latestPosts %}
                <p class="date">{{ post.dateAdded|date("d/m/Y \a\t H:i") }}</p>
                <p class="postTitle">🎫 <span class="lPtitle">{{ post.title }}</span> by 
                    <span class="lPuserName">🥳 {{ post.user.userName }}</span>
                </p>
                {% endfor %}
            </ol>
    </div>
    <div class="leaderBoard">
        <h2>🎉 Leaderboard</h2>
        <ol>
            {% for user in leaderBoard %}
                <li>{{ user.userName }} | {{ user.experience }}</li>
            {% endfor %}
        </ol>
    </div>
</div>
{% endblock %}
