{% block postStat %}
{# LeaderBoard #}
<style>
    .leaderBoard {
        font-family: var(--logo-font);
        margin-top: 3em;
        border: 1px solid white;
        width: 20em;
        text-align: center;
        margin-left: 1em;
    }

    .leaderBoard ol {
        list-style-type: none;
        padding: 0;
        margin-top: 1em;
        margin-bottom: 1em;
        line-height: 2em;
    }

    .leaderBoardUserName {
        color: rgb(66, 108, 245);
    }
</style>

{# Module #}
<div class="leaderBoard">
    <h2>🎉 leaderboard</h2>
    <ol>
        {% for user in leaderBoard %}
            <li><span class="leaderBoardUserName">{{ user.userName }}</span> | {{ user.experience }}</li>
        {% endfor %}
    </ol>
</div>
{% endblock %}
